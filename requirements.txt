-- Minimal requirements for Hugging Face Spaces (Gradio runtime)
# Keep the install lean to reduce build time and avoid native build failures
python-dotenv==1.0.0
requests==2.31.0
PyPDF2==3.0.1
python-docx==0.8.11
python-multipart==0.0.18
aiofiles==23.2.1
numpy==1.26.4
